/** 我的预留名称 **/
const IMPORT = 'IMPORT' // 导入
const MY_SERVICE = 'MY_SERVICE' // 服务
const DATA = 'DATA' // 数据
const MARK = 'MARK' // 标注
const MAP = 'MAP' // 地图
const SCENE = 'SCENE' // 场景
const BASE_MAP = 'BASE_MAP' // 地图
const SYMBOL = 'SYMBOL' // 符号
const TEMPLATE = 'TEMPLATE' // 模板
const MyColor = 'MyColor' // 色带

export default {
  IMPORT,
  MY_SERVICE,
  DATA,
  MARK,
  MAP,
  SCENE,
  BASE_MAP,
  SYMBOL,
  TEMPLATE,
  MyColor,
}
